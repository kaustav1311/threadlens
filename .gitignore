node_modules/
__pycache__/
*.egg-info/
.pytest_cache/
.venv/
# Never commit real chats
*.zip
chats/
private/
!samples/*.txt

# local preview config, not part of the project
.claude/

# Local labelling output. Derived from a real chat, so it never gets committed.
dev/out/

# The local-model build can reach the network. It is for your machine only and is
# never deployed: Pages publishes dist/index.html.
dist/index-local.html
